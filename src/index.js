import React from 'react';
import ReactDOM from 'react-dom/client';
import MoneyMindsetQuiz from './MoneyMindsetQuiz';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MoneyMindsetQuiz />
  </React.StrictMode>
);
